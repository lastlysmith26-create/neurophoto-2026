# 🤖 Handover Documentation: NeuroPhoto Project

## 🔍 Проект: NeuroPhoto SaaS
Это веб-приложение для генерации **профессиональных фотосессий людей и продуктов** с использованием AI (Gemini).

### 🎯 Задача от пользователя
1.  **Полный редизайн (UI/UX)**:
    *   Сделать "дорого-богато", использовать **Glassmorphism**, неоновые акценты (Cyan/Teal), темную тему.
    *   Добавить **шейдерную анимацию** на фон (как живой организм).
    *   Все элементы должны быть полупрозрачными, размытыми (backdrop-blur), с эффектом свечения.
2.  **Трансформация в SaaS**:
    *   Приложение должно работать **в облаке** (доступно по ссылке 24/7).
    *   **Многопользовательский режим**: Каждый юзер видит только свои модели и фото.
    *   Регистрация/Вход через Email.

### 🛠️ Технический стек (Реализовано)
*   **Frontend**: React + Vite + Tailwind CSS v4.
    *   **Three.js**: Используется для фона (`ShaderAnimation.jsx`).
    *   **Framer Motion**: Анимации интерфейса.
    *   **Lucide React**: Иконки.
*   **Backend**: Node.js + Express (API).
    *   **Google Gemini API**: Используется для генерации картинок (`gemini-pro-vision`).
*   **База данных и Auth**: **Supabase**.
    *   **PostgreSQL**: Хранение моделей и истории (`profiles`, `models`, `generations`).
    *   **Storage**: Хранение картинок (бакет `images`).
    *   **Auth**: Регистрация/Логин (Email + Password).

### 📥 Что было сделано (Status Update)
1.  **Дизайн (Готово ✅)**:
    *   Переписан `index.css`: глобальные стили Glassmorphism, переменные цветов.
    *   Создан компонент `ShaderAnimation.jsx` (Three.js GPGPU simulation).
    *   Переверстаны все страницы: `PhotoSession` (генерация), `ModelCreator` (создание аватаров), `Gallery`.
2.  **Бэкенд (Миграция на Supabase ✅)**:
    *   Локальные JSON-файлы (`models.json`, `history.json`) **удалены** из логики.
    *   Все эндпоинты (`models.js`, `generate.js`, `history.js`) переписаны на использование **Supabase Client**.
    *   Добавлена проверка JWT токена (RLS - Row Level Security), чтобы юзеры не видели чужие данные.
3.  **Фронтенд Логика (Готово ✅)**:
    *   Внедрен `AuthContext` и `AuthProvider`.
    *   Создана страница `Login.jsx` (красивая, glassmorphism).
    *   Настроены `Protected Routes` (нельзя зайти без логина).
    *   API Client (`client.js`) теперь автоматически добавляет токен авторизации к запросам.

### 🚧 Текущая проблема (На чем остановились)
*   **Локальный запуск**: Я пытался запустить серверы командой `npm run dev` (Front) и `node server.js` (Back), но пользователь сообщил, что "ничего не открылось" или "не перебросило".
*   **Верификация**: Нужно убедиться, что приложение запускается локально на `localhost:5173`, редиректит на `/login`, и после входа корректно работает с Supabase.
*   **Деплой**: Финальная цель — задеплоить Фронт на Vercel, а Бэк на Render/Railway. Это еще не сделано.

### 🔑 Данные доступа (Supabase)
В проекте настроены `.env` файлы с ключами:
*   URL: `https://iizmjkbdufjxxteklmib.supabase.co`
*   Key: `sb_publishable_...` (в коде)

### � План действий для следующего разработчика (Claude)
1.  **Диагностика запуска**: Проверить, почему локально не открывается (порты, ошибки в консоли). Запустить серверы.
2.  **Проверка БД**: Убедиться, что таблицы в Supabase (models, generations) созданы (скрипт `supabase_schema.sql` был передан пользователю).
3.  **Тест функционала**:
    *   **ВАЖНО**: Выполнить скрипт `storage_policy.sql` в Supabase SQL Editor (настройка прав доступа к файлам).
    *   Зарегистрироваться.
    *   Создать модель -> Проверить, что она появилась в таблице `models` в Supabase.
    *   Сгенерировать фото -> Проверить, что файл упал в Storage `images`.
4.  **Деплой**:
    *   Залить код на GitHub.
    *   Настроить Deploy на Vercel (Front) и Render (Back).
    *   Прописать переменные окружения в облаке.
